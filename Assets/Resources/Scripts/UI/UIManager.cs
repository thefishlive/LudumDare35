using UnityEngine;
using UnityEngine.UI;
using System.Collections;

public class UIManager : MonoBehaviour
{
    public GameObject ShiftProcessBar = default(GameObject);
    public GameObject AttackTimeBar = default(GameObject);
    public GameObject HealthBar = default(GameObject);

    public Text ScoreText = default(Text);
}
